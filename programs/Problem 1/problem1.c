#include<stdio.h>
int main()
{
        int length;// LENGTH OF THE ARRAY IS ST>
        scanf("%d",&length);
        int arr[length]; // ARRAY ELEMENTS ARE >
        for(int i=0;i<length;i++)
                scanf("%d",&arr[i]);
    //START WRITING YOUR CODE FROM HERE
        return 0;
}
