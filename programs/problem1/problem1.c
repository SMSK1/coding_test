#include<stdio.h>
int main()
{
	int length;// LENGTH OF THE ARRAY IS STORED IN length
	scanf("%d",&length);
	int arr[length]; // ARRAY ELEMENTS ARE STORED IN arr
	for(int i=0;i<length;i++)
      	scanf("%d",&arr[i]);
	//START WRITING YOUR CODE FROM HERE
	return 0;
}